apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: auth
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - auth-gateway-secrets.sealed.yaml
  - auth-admin-user.sealed.yaml
configMapGenerator:
  - name: auth-gateway-config
    files:
      - configuration.yaml
