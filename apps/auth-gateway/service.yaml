apiVersion: v1
kind: Service
metadata:
  name: auth-gateway
  namespace: auth
  labels:
    app: auth-gateway
spec:
  selector:
    app: auth-gateway
  ports:
    - name: http
      port: 80
      targetPort: 9091
      protocol: TCP

