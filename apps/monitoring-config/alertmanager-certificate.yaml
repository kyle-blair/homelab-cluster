apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: alertmanager-cert
spec:
  commonName: alertmanager.0x42labs.net
  dnsNames:
  - alertmanager.0x42labs.net
  duration: 8760h
  issuerRef:
    kind: ClusterIssuer
    name: internal-ca-issuer
  renewBefore: 720h
  secretName: alertmanager-tls
