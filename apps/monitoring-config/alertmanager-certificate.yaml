apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: alerts-cert
spec:
  commonName: alerts.0x42labs.net
  dnsNames:
  - alerts.0x42labs.net
  duration: 8760h
  issuerRef:
    kind: ClusterIssuer
    name: internal-ca-issuer
  renewBefore: 720h
  secretName: alerts-tls
