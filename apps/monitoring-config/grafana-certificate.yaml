apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: observability-cert
spec:
  commonName: observability.0x42labs.net
  dnsNames:
  - observability.0x42labs.net
  duration: 8760h
  issuerRef:
    kind: ClusterIssuer
    name: internal-ca-issuer
  renewBefore: 720h
  secretName: observability-tls
