apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
data:
  url: https://argocd.home.arpa
  admin.enabled: "false"
  oidc.config: |
    name: Auth Gateway
    issuer: https://auth.home.arpa
    clientID: argocd
    enablePKCEAuthentication: true
    requestedScopes:
      - openid
      - profile
      - email
      - groups
