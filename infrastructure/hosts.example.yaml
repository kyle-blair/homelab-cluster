all:
  vars:
    kube_proxy_strict_arp: true

  hosts:
    node0:
      ansible_host: 10.0.20.20
      ansible_user: operator
    node1:
      ansible_host: 10.0.20.21
      ansible_user: operator

  children:
    kube_control_plane:
      hosts:
        node0:

    kube_node:
      hosts:
        node0:
        node1:

    etcd:
      hosts:
        node0:

    k8s_cluster:
      children:
        kube_control_plane:
        kube_node:

    calico_rr:
      hosts: {}
